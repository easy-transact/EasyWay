venv/
__pycache__/
*.pyc
.env
db.sqlite3
*.sqlite3
staticfiles/
media/

# Celery beat's local schedule state (celery -A config beat, run locally
# during dev) -- runtime artifact, not project source, was accidentally
# tracked before this line existed.
celerybeat-schedule*
test-ui/*
# Routing spike: OSM extracts and built Valhalla tiles are large binaries,
# regenerated locally from docker-compose.yml, never committed.
valhalla/custom_files/*
!valhalla/custom_files/.gitkeep
